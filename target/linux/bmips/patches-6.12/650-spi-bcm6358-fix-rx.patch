--- a/drivers/spi/spi-bcm63xx.c
+++ b/drivers/spi/spi-bcm63xx.c
@@ -247,6 +247,7 @@ static int bcm63xx_txrx_bufs(struct spi_
 
 		if (t->rx_buf) {
 			do_rx = true;
+			memset_io(bs->tx_io + len, 0xFF, t->len);
 			/* prepend is half-duplex write only */
 			if (t == first)
 				prepend_len = 0;
