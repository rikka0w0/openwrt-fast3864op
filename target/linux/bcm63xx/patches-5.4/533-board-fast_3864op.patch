--- a/arch/mips/bcm63xx/boards/board_bcm963xx.c
+++ b/arch/mips/bcm63xx/boards/board_bcm963xx.c
@@ -2975,6 +2975,37 @@ static struct board_info __initdata boar
 		},
 	},
 };
+
+static struct board_info __initdata board_fast3864op = {
+	.name = "F@ST 3864OP",
+	.expected_cpu_id = 0x63268,
+
+	.has_pci = 1,
+	.has_ohci0 = 1,
+	.has_ehci0 = 1,
+	.num_usbh_ports = 2,
+
+	.has_enetsw = 1,
+	.enetsw = {
+		.used_ports = {
+			[3] = {
+				.used = 1,
+				.phy_id = 4,
+				.name = "WAN",
+			},
+			[4] = {
+				.used = 1,
+				.phy_id = 0,
+				.bypass_link = 1,
+				.force_speed = 1000,
+				.force_duplex_full = 1,
+				.mii_override = 1,
+				.timing_sel = 1,
+				.name = "RGMII",
+			},
+		},
+	},
+};
 #endif /* CONFIG_BCM63XX_CPU_63268 */
 
 /*
@@ -3089,6 +3120,7 @@ static const struct board_info __initcon
 	&board_H500s,
 	&board_VG8050,
 	&board_VR3032u,
+	&board_fast3864op,
 	&board_vw6339gu,
 #endif /* CONFIG_BCM63XX_CPU_63268 */
 };
@@ -3212,6 +3244,7 @@ static struct of_device_id const bcm963x
 	{ .compatible = "brcm,bcm963269bhr", .data = &board_963269bhr, },
 	{ .compatible = "comtrend,vg-8050", .data = &board_VG8050, },
 	{ .compatible = "comtrend,vr-3032u", .data = &board_VR3032u, },
+	{ .compatible = "sagem,fast-3864op", .data = &board_fast3864op, },
 	{ .compatible = "inteno,vg50", .data = &board_vw6339gu, },
 	{ .compatible = "sercomm,h500-s-lowi", .data = &board_H500s, },
 	{ .compatible = "sercomm,h500-s-vfes", .data = &board_H500s, },
